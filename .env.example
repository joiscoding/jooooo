# Optional: JSON endpoint returning Look[] (same shape as src/data/seedLooks.json).
# If unset or the request fails, the app uses built-in seed data.
# VITE_LOOKS_API_URL=https://example.com/looks.json
